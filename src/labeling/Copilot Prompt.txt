Use the taxonomy in Taxonomy.txt and the labeling examples in reddit_comments_data_hand_labeled.csv as references. For each row in the 'body' column of the input dataset, label with one label from Taxonomy.txt. Use the explanations and rules in Taxonomy.txt to complete this task, but only assign the label name that appears before the dash in the taxonomy entry. Add a new column named 'label' with the assigned label for each row. Return a downloadable csv file of the original data plus the labeled data column.